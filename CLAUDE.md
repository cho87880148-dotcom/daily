# 오늘 (daily)

혼자 쓰려고 만든 개인용 앱. **날씨 · 지금 뜨는 검색어 · 로또 번호 뽑기** 세 가지.
아이폰에서 "홈 화면에 추가" 해서 앱처럼 쓰는 방식(PWA)이다.

> 옆 폴더 `projects\test`(바이낸스 길잡이 / bnguide.co.kr)와
> `projects\newhome`(비트뉴스 / coinfocus.co.kr)은 **완전히 다른 사이트**다. 섞지 말 것.

**배포하지 않는다.** 스토어에 올리지 않으므로 개발자 계정·심사·유지보수 의무가 전부 없다.

---

## 사용자에 대해

**개발자가 아니다.** 설명은 한국어로, 전문용어는 풀어서.
시키는 대로 고치기 전에 해당 파일을 직접 읽어 전제가 맞는지 먼저 확인한다.

---

## 명령어

```powershell
.\serve.ps1         # 미리보기 → http://localhost:8090  (Ctrl+C 로 종료)
.\fetch-trends.ps1  # 인기 검색어 갱신 → data\trends.json
.\fetch-draws.ps1   # 로또 당첨번호 갱신 → data\draws.json  (아래 5번 참고)
.\make-icons.ps1    # 홈 화면 아이콘 다시 만들기 (한 번만 하면 됨)
```

### 아이폰에서 보려면

`serve.ps1` 을 **관리자 권한 PowerShell** 에서 실행해야 한다. 그래야 폰이 접속할 수 있는
`http://192.168...` 주소가 뜬다. 일반 권한으로 실행하면 localhost 전용으로 열리고,
그 주소는 PC 에서만 열린다. 폰과 PC 가 **같은 와이파이**여야 한다.

---

## 파일 구조

| 파일 | 무엇 |
|---|---|
| `index.html` | 화면 뼈대 |
| `app.css` | 색·모양. 색은 맨 위 `:root` 에 다 모아뒀다 |
| `app.js` | 날씨·검색어·로또 전부. 외부 라이브러리 없음 |
| `manifest.json` + `icon-*.png` | 홈 화면 아이콘 설정 |
| `sw.js` | 인터넷 끊겨도 열리게 하는 부분 |
| `data/trends.json` | 인기 검색어 (fetch-trends.ps1 이 만듦) |
| `data/draws.json` | 로또 당첨번호 (fetch-draws.ps1 이 만듦) |

---

## 1. 날씨 — Open-Meteo, API 키 없음

`api.open-meteo.com` 을 브라우저가 직접 부른다. **가입도 키도 필요 없다.**
서울·인천·부산 위경도는 `app.js` 맨 위 `CITIES` 에 있다. 도시를 추가하려면
거기에 한 줄 넣고 `index.html` 의 `city-tabs` 에 버튼을 하나 넣으면 된다.

처음엔 기상청 공공데이터포털을 쓸까 했지만, 그쪽은 **신청·승인·API 키**가 필요하고
격자 좌표(nx, ny)로 변환해야 해서 개인용으로는 Open-Meteo 가 훨씬 낫다.

## 2. 인기 검색어 — 구글 트렌드 RSS

**네이버는 2021년에 실시간 검색어를 완전히 폐지했고 대체 API 도 없다.**
사용자가 말한 "시그널" 류 사이트도 공개 API 가 없어 긁어오는 수밖에 없는데 잘 깨진다.
그래서 `trends.google.co.kr/trending/rss?geo=KR` (한국 1~10위)을 쓴다. 무료, 키 없음.

**브라우저에서 직접 못 부른다(CORS).** 그래서 `fetch-trends.ps1` 이 미리 받아
`data/trends.json` 으로 저장하고, 앱은 그 파일만 읽는다.
→ **자동으로 갱신되지 않는다.** 최신으로 보려면 스크립트를 실행해야 한다.

## 3. 로또 뽑기 기준

`app.js` 의 `drawSmart()`. 아래를 **전부** 만족할 때까지 다시 뽑는다.
무작위 조합의 **약 50%가 통과**한다 (3만 회 모의실험으로 확인). 너무 빡빡하지도 헐겁지도 않다.

| 조건 | 막으려는 것 |
|---|---|
| 다섯 구간 중 3구간 이상 | 한 구간 몰림 |
| 홀짝 2:4 ~ 4:2 | 홀·짝 쏠림 |
| 낮은수(1~22)·높은수(23~45) 각 2개 이상 | 앞뒤 쏠림 |
| 합계가 역대 중앙 90% 구간 | 극단적인 합 |
| 3개 이상 연속 금지 | 5,6,7 같은 줄번호 |
| 같은 끝자리 3개 이상 금지 | 3,13,23 |
| 32 이상 최소 1개 | ↓ 아래 설명 |

### ★ 바꾸지 말 것 — 정직성

**로또는 매 회차가 완전히 독립이다. 위 조건 중 마지막 하나를 뺀 전부는
당첨 확률을 1%도 올리지 않는다.** 조합의 "모양"을 고르게 만들 뿐이다.

`32 이상 최소 1개` 만 성격이 다르다. 1등 당첨금은 당첨자끼리 나눠 갖는데
사람들이 생일 때문에 1~31 에 몰려 있어서, 32~45 가 섞이면 **당첨될 경우
나눠 갖는 인원이 줄어든다. 확률이 아니라 실수령액이 올라가는 것이다.**

이 구분은 화면(`index.html` 의 "읽어두세요" 칸, 뽑기 결과의 마지막 줄)에
그대로 적혀 있다. **이 문구를 빼거나 "확률이 올라간다"는 식으로 바꾸지 말 것.**
본인이 만든 도구라서 더 믿게 되는 게 이 앱의 유일한 실질적 위험이다.

## 4. 운세는 뺐다

원래 띠별 운세도 넣으려다 사용자가 뺐다. 다시 넣게 되면:
**공짜로 쓸 수 있는 운세 데이터가 없다.** 네이버·다음은 유료 제휴 콘텐츠라 못 쓴다.
직접 문장 풀을 만들어 날짜+띠로 조합하거나, AI 로 매일 생성하는 두 가지 길뿐이다.

## 5. ★ 로또 당첨번호 자료가 아직 없다

`fetch-draws.ps1` 이 쓰던 동행복권 JSON API(`common.do?method=getLottoNumber`)를
**동행복권이 막았다.** 2026-08-06 확인 — JSON 대신 홈페이지 HTML 이 온다.
`gameResult.do` 쪽 주소들도 전부 같은 페이지로 막힌다. User-Agent·세션 쿠키를 넣어도 안 된다.

스크립트는 막힌 걸 감지하면 안내만 하고 안전하게 끝난다. 자료를 넣으려면
동행복권 사이트에서 **엑셀을 직접 내려받아** `data\` 에 넣고 파서를 붙여야 한다.

**이 자료가 없어도 앱은 정상이다.** 통계 칸만 안 보이고, 합계 기준은 기본값 100~175 를 쓴다.

---

## 앞으로 할 만한 것

- **인기 검색어 자동 갱신** — 지금은 손으로 `fetch-trends.ps1` 을 돌려야 한다.
  GitHub Pages 에 올리고 Actions 를 붙이면 비트뉴스처럼 자동이 된다.
  (다만 GitHub Pages 는 공개다. 개인 자료가 없으니 상관은 없다.)
- 로또 당첨번호 엑셀 파서
- 뽑은 번호를 저장해두고 다시 보기
